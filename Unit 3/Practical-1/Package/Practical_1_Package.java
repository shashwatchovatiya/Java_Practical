package Package;

public class Practical_1_Package {
    public int findmax(int num1, int num2, int num3) {
        // Find the maximum of three numbers
        return Math.max(num1, Math.max(num2, num3));
    }
}
